<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.plugin.MethodInfoMapper">

    <insert id="insert" parameterType="java.util.List">
        INSERT INTO method_info (id, description, method_class_id, name, return_type, return_description)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.description} ,#{item.methodClassId}, #{item.name}, #{item.returnType}, #{item.returnDescription})
        </foreach>
    </insert>
    <update id="update" parameterType="java.util.List">
        UPDATE method_info
        SET description = case id
            <foreach collection="list" item="item">
                when #{item.id} then #{item.description}
            </foreach>
        end,
        return_description = case id
            <foreach collection="list" item="item">
                when #{item.id} then #{item.returnDescription}
            </foreach>
        end
        WHERE id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>
</mapper>