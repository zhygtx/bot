<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.generalbot.plugin.mapper.AttributeMapper">
    <insert id="insert" parameterType="java.util.List">
        INSERT INTO attribute (id, description, entity_info_id, type, name)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.description}, #{item.entityInfoId}, #{item.type}, #{item.name})
        </foreach>
    </insert>
    <update id="update" parameterType="java.util.List">
        UPDATE attribute
        SET description = case id
            <foreach collection="list" item="item">
                when #{item.id} then #{item.description}
            </foreach>
        end
        WHERE id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>
</mapper>
