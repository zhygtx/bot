<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.generalbot.workflow.mapper.DataMapMapper">

    <insert id="insert" parameterType="java.util.List">
        insert into data_map(id, node_id, source_node_id, source_path, target_param_name, param_index, target_path, source_type, target_type)
        values
        <foreach item="item" index="index" collection="list" separator="," >
            (#{item.id}, #{item.nodeId}, #{item.sourceNodeId}, #{item.sourcePath}, #{item.targetParamName}, #{item.paramIndex}, #{item.targetPath}, #{item.sourceType}, #{item.targetType})
        </foreach>
    </insert>
</mapper>