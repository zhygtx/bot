<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.workflow.DataMapMapper">

    <insert id="insert" parameterType="java.util.List">
        insert into data_map(id, node_id, source_node_id, source, target, source_type, target_type)
        values
        <foreach item="item" index="index" collection="list" separator="," open="(" close=")">
            (#{item.id}, #{item.nodeId}, #{item.sourceNodeId}, #{item.source}, #{item.target}, #{item.sourceType}, #{item.targetType})
        </foreach>
    </insert>
</mapper>