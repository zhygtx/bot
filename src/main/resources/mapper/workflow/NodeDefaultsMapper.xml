<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.workflow.NodeDefaultsMapper">

    <insert id="insert" parameterType="java.util.List">
        insert into node_defaults(id, node_id, param_index, param_name, field_path, default_value, default_value_type)
        values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.id}, #{item.nodeId}, #{item.paramIndex}, #{item.paramName}, #{item.fieldPath}, #{item.defaultValue}, #{item.defaultValueType})
        </foreach>
    </insert>
</mapper>